# Copy to .env for local runs, or set these in your host's dashboard.
# .env is gitignored - never commit a real key.

# Port to bind (Railway sets this automatically)
PORT=5000

# How often the background job refetches every symbol, in seconds
REFRESH_SECONDS=90

# Optional: enables the AI assistant button. Leave empty to disable it.
GEMINI_API_KEY=
